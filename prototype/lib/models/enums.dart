enum UnitType {
  mass, // g, kg, oz, lb
  volume, // ml, l, tsp, tbsp, cup, fl oz
  count, // piece, clove, slice
  misc // pinch, handful
}

enum SystemOfMeasurement { metric, imperial }

enum Cuisine { thai, mexican, vegan, italian, western, asian, other }

enum SessionStatus { setup, inProgress, paused, completed, abandoned }
